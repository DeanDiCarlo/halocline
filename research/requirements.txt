# Local packages
-e ./halocline-physics
-e ./halocline-surrogate[report,test]

# Explicit training stack. The surrogate package also declares these, but
# keeping them visible here makes the one-command setup easier to inspect.
torch>=2.2
h5py>=3.10
numpy>=1.26
scipy>=1.12
PyYAML>=6
pytest>=8
matplotlib>=3.8
